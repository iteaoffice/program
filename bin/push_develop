#!/usr/bin/env sh
SRC_DIR="`pwd`"
cd "`dirname "$0"`"
cd ".."

git push origin develop --tag
git push origin master

REV="`git rev-parse HEAD`"

php ocular.phar code-coverage:upload --format=php-clover --repository=g/debranova/program --revision="$REV"  clover.xml
