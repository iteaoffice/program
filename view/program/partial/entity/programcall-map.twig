<script type="text/javascript" src="https://www.google.com/jsapi"></script>
<script type="text/javascript">

    google.load('visualization', '1.1', {'packages': ['geomap']});

    google.setOnLoadCallback(drawRegionsMap);
    function drawRegionsMap() {
        var data = google.visualization.arrayToDataTable([
            ['Country', 'Project Partners'],
            {% for country in countries %}
            ['{{ country.country }}', 1],
            {% endfor %}
        ]);
        var options = {
            region: '150',
            displayMode: 'auto',
            enableRegionInteractivity: 'false',
            legend: 'none',
            colorAxis: {colors: ['#009ac9', '#009ac9', '#009ac9', '#009ac9', '#009ac9']}
        };
        var chart = new google.visualization.GeoChart(document.getElementById('organisation_map'));
        chart.draw(data, options);
    }
</script>
<div class="col-md-12" >
    <div id="organisation_map" style=""></div>
</div>
