<h2>{{ translate("txt-call-selector") }}</h2>

<ul class="nav nav-pills">
    {% for call in calls|reverse %}
        <li {% if callId == call.id %}class="active"{% endif %}>{{ callLink(call, 'view-list', 'name') }}</li>
    {% endfor %}
</ul>